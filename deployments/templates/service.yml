apiVersion: v1
kind: Service
metadata:
  name: laa-portal-stabilisation-prototype-service
spec:
  selector:
    app: laa-portal-stabilisation-prototype # this should match the pod label in deployment.yml
  ports:
    - name: https
      port: 8080
      targetPort: 8080
